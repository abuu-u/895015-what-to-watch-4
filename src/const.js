const TAB = {
  overview: `Overview`,
  details: `Details`,
  reviews: `Reviews`,
};

const TABS = Object.values(TAB);

const DEFAULT_GENRE = `All genres`;

export {
  TAB,
  TABS,
  DEFAULT_GENRE,
};
